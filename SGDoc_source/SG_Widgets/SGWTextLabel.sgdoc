@SG_CPP_CLASS SGWTextLabel
@SG_SHORT_DESCRP @SG_FINDREP_WIDGET label that displays text
@SG_LONG_DESCRP SGWTextLabel provides a @SG_FINDREP_WIDGET non interactive label that displays text.
@SG_IMPLE Stores information about the SGWTextLabel.
@SG_FUNC SGWTextLabel(SGWWidget* parent, const SGXString& s, float x1, float x0, float y1, float y0, float w1, float w0, float h1, float h0, SGWHorizontalAlignment::Flag alignment, bool isWarning);
Creates a SGWTextLabel with parent @SG_FINDREP_WIDGET $parent, @SG_FINDREP_WDIST coordinates ($x1, $x0, $y1, $y0, $w1, $w0, $h1, $h0), and horizontal text alignment mode $alignment. If $isWarning is false, the SGWTextLabel is created normally, otherwise it is seen as a warning for invalid input, so it would have a darker colour and would be hidden upon creation.
@SG_WARN $parent being nullptr is undefined behaviour.
