@SG_CPP_CLASS SGWTextButton
@SG_SHORT_DESCRP @SG_FINDREP_WIDGET button displaying text
@SG_LONG_DESCRP SGWTextButton provides a @SG_FINDREP_WIDGET button that displays text. The button sends out @SG_FUNDREP_FUNCPTR when pressed.
@SG_IMPLE Stores information about the SGWTextButton.
@SG_FUNC SGWTextButton(SGWWidget* parent, const SGXString& s, void (*attachedFunction)(), float x1, float x0, float y1, float y0, float w1, float w0, float h1, float h0);
Creates a SGWIconButton with parent @SG_FINDREP_WIDGET $parent, @SG_FINDREP_WDIST coordinates ($x1, $x0, $y1, $y0, $w1, $w0, $h1, $h0), @SG_FINDREP_FUNCPTR when clicked as $attachedFunction, and text displayed $s.
@SG_WARN $parent being nullptr is undefined behaviour.
